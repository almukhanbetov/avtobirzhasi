.env
/bin/

# Local-dev listing-photo uploads (UPLOAD_DIR default). Production keeps
# these on a Docker volume — see docker-compose.prod.yml.
/uploads/

# Stray local debug binary (unrelated FreedomPay investigation) found
# sitting untracked in this checkout — not project output, never meant to
# be committed. See docs/DEPLOYMENT_PLAN.md's Git-readiness findings.
/sandboxcheck
